# Flutter plugins register through generated code. Keep annotations used by the
# generated Retrofit/JSON boundary while allowing the remainder to be optimized.
-keepattributes *Annotation*
-keepattributes Signature
