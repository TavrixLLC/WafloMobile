# Environments

Development, staging, and production have distinct native identifiers and labels. Production/staging require HTTPS/non-local hosts; committed `.invalid` values must be replaced by approved deployment configuration. W4 `test` is the documented internal pairing value for product staging. See `docs/m1/environments.md`.
