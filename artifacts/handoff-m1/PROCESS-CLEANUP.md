# Process cleanup

Quality-gate completion checks for lingering Flutter, Dart, Gradle, emulator, Java build, and test processes started by this task. No background backend or provider runtime is bundled. A final snapshot is recorded in `raw-test-output/quality-gates.txt`.
