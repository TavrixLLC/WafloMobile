# Screenshot manifest

All images under `screenshots/` are generated from `test/golden/m1_screenshot_test.dart` at 390×844 using synthetic non-credential context. The scanner image replaces only the live camera texture with a safe preview while rendering the production scanner screen. `contact-sheet.png` summarizes the set and `checksums.sha256` records each PNG.

The numbered set covers English/Arabic welcome, camera rationale, scanner, invalid/expired pairing, progress, success, home, assigned location count, English/Arabic settings, offline, expired session, revoked, compromised, update required, text scaling, dark theme, and semantics debugger.
