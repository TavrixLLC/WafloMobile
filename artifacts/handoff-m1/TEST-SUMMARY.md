# Test summary

Local unit/widget/contract-fixture/golden tests passed: 58 passed and one opt-in real-backend test skipped. Android build gates are recorded in `raw-test-output/quality-gates.txt`. The real backend contract test was skipped because no seeded local W4 service/one-time QR was supplied. Device integration execution requires an attached Android/iOS runner; GitHub Actions supplies an Android emulator.

iOS cannot be compiled on this Windows host. Exact macOS CI results must be appended after the first external workflow run; no pass is claimed here.
