# Raw safe output

`flutter-test.txt` is emitted directly by the Flutter test runner. `quality-gates.txt` records exit codes and platform limitations for the remaining gates. Files are reviewed for secrets before handoff.
