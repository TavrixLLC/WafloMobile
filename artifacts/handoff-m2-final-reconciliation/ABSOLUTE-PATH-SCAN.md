# Absolute-path scan

Committed M1/M2 evidence was redacted without removing useful results. The portable scanner checks tracked text for personal Windows, macOS, and Linux home paths plus Flutter/Android SDK assignments and known local SDK locations.

Narrow source-code allowlisting is limited to the scanner's own pattern definitions. The local tracked-tree scan passed; the portable archive is scanned separately after creation.
