# Screenshot manifest

`screenshots/` contains 25 real Flutter state screenshots plus `00-contact-sheet.png`. The set covers every M2 evidence item requested and contains only sanitized deterministic fixture data; no valid QR, credential, token, real person, purchase reference, or camera capture appears.

Exact SHA-256 values are in `screenshots.sha256`.
