# Two-state stamp grid

The only states are filled and empty. Program artwork is digest-verified and version-safe; the fallback remains visually filled versus empty. One semantic summary replaces per-slot announcements. See `docs/m2/stamp-grid.md` and screenshots 04–06/16.
