# MOB-001 — stamp Manager override removal

Status: `RESOLVED_MOBILE`.

Production-v1 does not permit Manager approval for stamping. `issueStamps` builds its serialized body from the QR payload, stamp amount, purchase fields, optional merchant reference, and idempotency key only. It has no `managerOverride` input or serialization branch.

The historical generated M2 model remains immutable and may still describe its historical optional field. Current Mobile transport does not instantiate or send it.

Evidence:

- `lib/features/membership_resolution/data/loyalty_operations_api.dart`
- `test/unit/loyalty_operations_api_test.dart`
- `tool/verify_production_v1_contract.dart`

The focused wire test decodes the actual captured stamp body and proves `managerOverride` is absent.
