# MOB-003 — authoritative billing denial

Status: `RESOLVED_MOBILE`.

`OPERATION_BILLING_BLOCKED` is treated as an authoritative server denial. Mobile does not mutate progress, reward readiness, or the stamp grid; it does not enqueue or retry a new business mutation. The retained membership projection is the last authoritative projection.

The Staff-facing state says that loyalty operations are paused, confirms that no customer progress changed, and directs an Owner to review billing in Merchant Web. Its primary action returns Home instead of suggesting another scan or a local override.

Focused controller and screenshot tests prove projection preservation and safe recovery copy.
