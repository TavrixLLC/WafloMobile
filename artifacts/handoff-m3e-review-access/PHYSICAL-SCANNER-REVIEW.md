# Physical scanner review

`flutter devices` found no Android/iOS hardware and no supported Android emulator during this pass. Therefore camera startup, system permission UI, torch hardware, optical detection, background/resume, and physical screenshots are **NOT VERIFIED**.

The staging debug APK is built and installable with debug signing. Required owner checks: first-run permission, permanent denial/settings recovery, beam smoothness, torch on/off and teardown, valid/invalid/repeated QR, resolve transition, background/resume, Arabic, dark mode, and 200% text.

Physical iOS verification is not claimed.
