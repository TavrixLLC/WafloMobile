# Sanitized UI reference

This directory intentionally contains structural notes rather than copied production Dart. Networking, repositories, authentication, storage, signing, and backend contracts are excluded.

Presentation hierarchy: shell → screen state → reusable Waflo component → semantic token. Scanner presentation is shared across normal and Demo; capability adapters change input/control authority without changing chrome.
