# ADR 006: Environment isolation

Status: accepted.

Use native flavors/schemes, distinct identifiers and labels, build-time API hosts, and QR-environment validation. Production rejects unsafe values at boot. W4 `test` is the documented adapter for product staging.
